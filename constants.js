const DB_USER= "darigomez30"
const DB_PASSWORD = "300690"
const DB_HOST = "cluster0.pbin7mk.mongodb.net"

const API_VERSION = "v1"
const IP_SERVER = "localhost"

const JWT_SECRET_KEY = "dsef"

module.exports = {
    DB_USER,
    DB_PASSWORD,
    DB_HOST,
    API_VERSION,
    IP_SERVER,
    JWT_SECRET_KEY 
}